name: capyba_app
description: App for technical challenge.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  camera_camera: ^2.0.1
  get: ^4.1.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  firebase_core: "^1.3.0"
  firebase_auth: "^2.0.0"
  cloud_firestore: "^2.3.0"
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true
